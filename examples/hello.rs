// Attributes - metadata for the compiler
#![allow(unused)]

// main () - entry point for the rust program
fn main() {
    // functions that end with a "!" is called a macro
    // Macros in rust generates code at compile time and are invoked with an exclamation mark
    // println! is a macro that prints text to the console
    println!("Hello, world!");
}